varying vec2			v_texCoord0;
varying float			v_depth;

void main (void) {
	v_texCoord0 = gl_MultiTexCoord0.xy;
	gl_FrontColor = gl_Color;
	v_depth = -(gl_ModelViewMatrix * gl_Vertex).z;
	gl_Position = gl_ModelViewProjectionMatrix * gl_Vertex;
}
